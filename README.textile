_test-simple.el_ allows you to create unit tests for your Emacs Lisp code in a very simple way. The system is designed to be useful both when run interactively or by a batch emacs invocation.

__To be continued....__
